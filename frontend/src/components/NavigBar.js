import React from 'react';
import { Navbar, Container } from 'react-bootstrap';

const NavigBar = () => {
  return (
    <Navbar bg="light" expand="lg">
      <Container>
        <Navbar.Brand>InIT</Navbar.Brand>
      </Container>
    </Navbar>
  );
};

export default NavigBar;
